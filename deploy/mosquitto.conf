# PiAssistant Mosquitto configuration
# Place in /etc/mosquitto/conf.d/piassistant.conf

# Listen on all interfaces (for Pico W access on local network)
listener 1883

# Allow anonymous for dev — add authentication before production
allow_anonymous true
